# homebrew-tap

Homebrew tap for `keep-codex-fast`.

```bash
brew install k0nkupa/tap/keep-codex-fast
keep-codex-fast setup
```

Development install:

```bash
brew install --HEAD k0nkupa/tap/keep-codex-fast
```
